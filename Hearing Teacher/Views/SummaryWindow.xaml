<Window x:Class="Hearing_Teacher.Views.SummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hearing_Teacher.Views" 
        xmlns:viewmodels="clr-namespace:HearingTeacher.ViewModels"
        mc:Ignorable="d"
        Title="Summary Window" MinHeight="550" MinWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="Margin" Value="10, 10" />
            </Style>
        </Grid.Resources>

        <StackPanel Grid.Column="0" CanVerticallyScroll="True">
            <Label Content="Your answers" />
            <ListView x:Name="yourAnswers" />
        </StackPanel>
        <StackPanel Grid.Column="1" CanVerticallyScroll="True">
            <Label Content="Correct answers" />
            <ListView x:Name="correctAnswers" />
        </StackPanel>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
    </Grid>
</Window>
